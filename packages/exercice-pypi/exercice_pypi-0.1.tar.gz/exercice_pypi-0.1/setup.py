from setuptools import setup

setup(name='exercice_pypi',
      version='0.1',
      description='Gaussian and Binominal distributions',
	  author= 'Karima Bouricha',
      packages=['exercice_pypi'],
      zip_safe=False)
