[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "mkmd"
version = "0.0.1b0"
description = "Create markdown documents"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
classifiers = []
