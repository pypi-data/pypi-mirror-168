[tool.poetry]
name = "my-custom-greeter"
version = "0.1.4"
description = ""
authors = ["sindbad_the_sailor <sindbad_the_sailor@example.com>"]
readme = "README.md"
packages = [{include = "my_custom_greeter"}]

[tool.poetry.scripts]
mcg = "my-custom-greeter.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.6.1"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
