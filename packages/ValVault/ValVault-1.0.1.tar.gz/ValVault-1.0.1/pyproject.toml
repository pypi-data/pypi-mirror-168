[project]
name = "ValVault"
version = "1.0.1"
readme = "README.md"
authors = [{ name = "PWall" }]
license = { file = "LICENSE" }
keywords = ["valorant", "vault", "keepass"]
dependencies = [
    "requests",
    "pykeepass",
    "jwt",
]
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/pwall2222/ValVault"
