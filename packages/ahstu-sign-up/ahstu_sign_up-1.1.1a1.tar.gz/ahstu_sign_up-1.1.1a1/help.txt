Windows:
    1. 在conf文件夹内找到.template文件, 复制一份并重新命名为xxx.ini
    2. 打开xxx.ini, 在其中填写个人的填报信息，并保存（不要改变文件字符编码）
    3. 双击sign_up.cmd（不建议直接双击其中的exe文件）

Linux:
    1. 下载源码后解压
    2. 根据需要自行创建python虚拟环境, 已提供requirements.txt
    3. 激活虚拟环境后运行./src/main.py 即可