[tool.poetry]
name = "diffren-jax"
version = "0.1.0"
description = ""
authors = ["fcole <fcole@google.com>"]
readme = "README.md"
packages = [{include = "diffren_jax"}]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
