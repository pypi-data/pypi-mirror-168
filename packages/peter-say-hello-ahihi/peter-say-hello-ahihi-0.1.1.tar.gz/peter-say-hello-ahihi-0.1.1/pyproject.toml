[tool.poetry]
name = "peter-say-hello-ahihi"
version = "0.1.1"
description = ""
authors = ["Peter <petnguyen@axon.com>"]
readme = "README.md"
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.scripts]
hello = "src.say_hello:say_hello"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
