{% for project in projects %}
* ({{ project.doc_id }}) {{ project.project }}{% for i in range(0, maxP - (project.project|length)) %} {% endfor %}{% if project.doc_id <10 %} {% endif %}v{{ project.version }}{% for i in range(0, maxV - (project.version|length)) %} {% endfor %} path: "{{ project.path }}"{% endfor %}
