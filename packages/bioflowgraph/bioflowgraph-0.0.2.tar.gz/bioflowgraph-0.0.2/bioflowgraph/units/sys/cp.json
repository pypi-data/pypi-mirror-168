{
    "executor": "cat",
    "parameters": [
        {
            "name": "source",
            "required": true,
            "is_input": true
        },
        {
            "name": "dest",
            "required": true,
            "is_output": true,
            "default": "${dest}"
        }
    ]
}